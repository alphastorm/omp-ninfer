# Role -> lane mapping for a two-agent fleet. Static pinning; one lane per agent. The RTX 3090
# scout role is deferred with its GPU and is not part of v0.7.3.
roles:
  main:
    provider: ninfer/local-max
    fragment: provider-5090.json
  heavy:
    provider: ninfer/local-max
    fragment: provider-4090.json
