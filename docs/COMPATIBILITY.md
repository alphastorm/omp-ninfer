<!-- Generated by scripts/render_compatibility.py from compatibility.json; do not edit. -->
# Compatibility matrix

Authority: `omp-ninfer-v0.1.0-beta.1-cross-platform-preview-5`  
Product release: `v0.1.0-beta.1`  
Composition: **blocked**

Client status is independent from the shared RTX 5090 runtime qualification. `preview` is not a support claim.

| Profile | Client | Runtime | Transport | Adapter | Status | Installable | Acceptance |
| --- | --- | --- | --- | --- | --- | --- | --- |
| `darwin-remote-ssh` | macOS arm64 | `qwen38-5090-v0.1.0` | `ssh-loopback` | `darwin-remote-ssh` | **preview** | no | pending |
| `windows-docker-local` | Windows 11 x64 | `qwen38-5090-v0.1.0` | `local-loopback` | `windows-docker-local` | **preview** | yes | pending |
| `linux-docker-local` | Linux x64 | `qwen38-5090-v0.1.0` | `local-loopback` | `linux-docker-local` | **preview** | yes | pending |

## Profile boundaries

### `darwin-remote-ssh`

Commands: `doctor`, `status`

Limitations:
- Managed remote install, benchmark, rollback, and support-bundle are not implemented; the existing manual SSH topology remains separate.
- Preview pending a clean managed macOS client acceptance receipt.

Blockers:
- No managed macOS acceptance receipt.
- The composed cross-platform lifecycle source remains draft and unmerged.

### `windows-docker-local`

Commands: `doctor`, `plan`, `install`, `status`, `benchmark`, `rollback`, `support-bundle`

Limitations:
- Native Windows OMP controls a Docker Desktop Linux container; native Windows NInfer and native Windows CUDA are unsupported.
- Preview pending a clean native Windows acceptance receipt.

Blockers:
- No clean native Windows acceptance receipt.
- The composed Windows client archive is unpublished.

### `linux-docker-local`

Commands: `doctor`, `plan`, `install`, `status`, `benchmark`, `rollback`, `support-bundle`

Limitations:
- Native Linux OMP requires a local Docker Linux engine and NVIDIA Container Toolkit.
- Preview pending a clean native Linux acceptance receipt.

Blockers:
- No clean native Linux acceptance receipt.
- The composed Linux client archive is unpublished.

## Composition blockers

- Record clean native Windows, native Linux, and managed macOS client acceptance receipts.
- Publish client distributions only after their exact acceptance receipts pass.
